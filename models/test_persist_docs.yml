version: 2

models:
  - name: test_persist_docs
    description: "This is my model-level description"
    config:
      alias: access_history
    columns:
      - name: id
        description: "Primary key"